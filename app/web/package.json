{
  "name": "@simoona/web",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "shell:check": "node ./scripts/verify-web-shell-foundation.mjs",
    "lint": "pnpm shell:check",
    "typecheck": "pnpm shell:check",
    "test": "pnpm shell:check",
    "build": "pnpm shell:check"
  }
}
