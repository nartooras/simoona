# QA Report

## 1) Decision

- Status: `GREEN`
- Scope: `R2-AUTH-REAL-001B` SQL-backed auth/session enforcement slice
- Phase: `R2`

## 2) Commands Executed

- `PNPM_FETCH_RETRIES=0 PNPM_FETCH_RETRY_MINTIMEOUT=1 PNPM_FETCH_RETRY_MAXTIMEOUT=1 pnpm --dir app install`
  - Result: `fail` (`ENOTFOUND registry.npmjs.org` in sandbox; package fetch retries still attempted)
- `pnpm --dir app/api lint`
  - Result: `pass`
- `pnpm --dir app/api typecheck`
  - Result: `pass`
- `pnpm --dir app/api test`
  - Result: `pass`
- `pnpm --dir app/api build`
  - Result: `pass`
- `pnpm --dir app/tests/parity runtime:api:auth`
  - Result: `pass` (run in unrestricted mode due sandbox localhost bind restriction)
- `pnpm --dir app/tests/parity runtime:api:wall-feed`
  - Result: `pass` (run in unrestricted mode due sandbox localhost bind restriction)
- `pnpm --dir app/tests/parity runtime:api:matrix`
  - Result: `pass` (run in unrestricted mode due sandbox localhost bind restriction)
- `pnpm --dir app verify`
  - Result: `pass`
- `git ls-files | rg '(^|/)node_modules/|(^|/)dist/|(^|/)bin/|(^|/)obj/'`
  - Result: `pass` (no tracked generated artifacts)

## 3) Findings

- `P2`: sandbox networking prevents reproducible install in constrained mode
  - Repro: run `pnpm --dir app install`
  - Expected vs actual: expected dependency fetch/install; actual DNS `ENOTFOUND` for npm registry.

- `P2`: sandbox localhost policy blocks runtime bind for runtime parity scripts
  - Repro: run `pnpm --dir app/tests/parity runtime:api:auth|runtime:api:wall-feed|runtime:api:matrix` without unrestricted mode.
  - Expected vs actual: expected direct localhost bind; actual `EPERM` in sandbox, requiring unrestricted execution.

## 4) Parity Impact

- Auth/session state now resolves from SQL-backed runtime tables.
- Unresolved `x-legacy-user-id` values are rejected as unauthorized.
- Runtime auth, wall-feed, and full API matrix parity checks pass with updated behavior.

## 5) Required Fixes

- none blocking for this slice

## 6) Retest Plan

- `pnpm --dir app/tests/parity runtime:api:auth`
- `pnpm --dir app/tests/parity runtime:api:wall-feed`
- `pnpm --dir app/tests/parity runtime:api:matrix`
- `pnpm --dir app verify`
