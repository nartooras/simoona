# QA Report

## 1) Decision

- Status: `GREEN`
- Scope: `R2-AUTH-REAL-001A` auth/token/session enforcement slice
- Phase: `R2`

## 2) Commands Executed

- `pnpm --dir app install`
  - Result: `fail` (`ENOTFOUND registry.npmjs.org` in sandbox)
- `pnpm --dir app lint`
  - Result: `pass`
- `pnpm --dir app typecheck`
  - Result: `pass`
- `pnpm --dir app test`
  - Result: `pass`
- `pnpm --dir app smoke`
  - Result: `pass` (fallback path due sandbox localhost bind restriction on `127.0.0.1:5173`)
- `pnpm --dir app build`
  - Result: `pass`
- `pnpm --dir app verify`
  - Result: `pass`
- `pnpm --dir app/api build`
  - Result: `pass`
- `pnpm --dir app/api lint`
  - Result: `pass`
- `pnpm --dir app/api typecheck`
  - Result: `pass`
- `pnpm --dir app/api test`
  - Result: `pass`
- `pnpm --dir app/tests/parity runtime:api:auth`
  - Result: `pass` (run in unrestricted mode due sandbox localhost bind restriction on `127.0.0.1:4313`)
- `git ls-files | rg '(^|/)node_modules/|(^|/)dist/|(^|/)bin/|(^|/)obj/'`
  - Result: `pass` (no tracked generated artifacts)

## 3) Findings

- `P2`: sandbox networking prevents reproducible install in constrained mode
  - Repro: `pnpm --dir app install`
  - Expected vs actual: expected dependency fetch/install; actual DNS `ENOTFOUND` for npm registry.

- `P2`: sandbox localhost policy blocks runtime bind for smoke/auth runtime tests
  - Repro: `pnpm --dir app smoke`, `pnpm --dir app/tests/parity runtime:api:auth`
  - Expected vs actual: expected direct localhost bind; actual `EPERM` in sandbox, requiring fallback or unrestricted execution.

## 4) Parity Impact

- Auth/token flow: runtime behavior now enforces login/user-info/logout/revocation path checks.
- Permission guard behavior: unauthenticated requests are rejected and missing-permission requests return denied behavior.
- Remaining parity gap: SQL-backed auth/session parity source is still open (`R2-AUTH-REAL-001B`).

## 5) Required Fixes

- none blocking for this slice

## 6) Retest Plan

- `pnpm --dir app/tests/parity runtime:api:auth`
- `pnpm --dir app verify`
- `pnpm --dir app smoke` (run unrestricted when direct localhost runtime bind evidence is required)
