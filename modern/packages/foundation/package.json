{
    "name": "@simoona/foundation",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "node --test test/**/*.test.js",
        "lint": "eslint \"src/**/*.ts\"",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
